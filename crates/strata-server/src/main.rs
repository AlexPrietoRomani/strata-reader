//! `strata-server` — Phase 0 skeleton. Full implementation lands in F9.T9.1.

#![deny(rust_2018_idioms)]

fn main() {
    eprintln!("strata-server v{} — not yet implemented (see Plan Maestro §14)", env!("CARGO_PKG_VERSION"));
    std::process::exit(2);
}
